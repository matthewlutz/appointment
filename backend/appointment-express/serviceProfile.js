
const express = require('express');
//const bcrypt = require('bcrypt');
const db = require('./appointment-database'); 
const router = express.Router();

router.post('/(idk)', async (req, res) => {
    
}

module.exports = router;